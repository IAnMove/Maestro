# Pending mount for H10 Video Editor HTTP extraction.
# Do not apply while mac PR #353 owns app/_launch_runtime.py.
# #366 also uses this filename for the wizard executor mount; concatenate
# both include_router blocks when applying.
# After apply, route ordinals stay: probe → probe-audio → thumbnail →
# screenshot → export → comics/animatic → export/{job_id} → cancel.
#
# Keep _run_comic_animatic and _resolve_comic_animatic_image in launch.
# Delete the duplicated editor job helpers and HTTP handlers from the
# "API Routes: lightweight video editor" block once these imports land.

diff --git a/app/_launch_runtime.py b/app/_launch_runtime.py
--- a/app/_launch_runtime.py
+++ b/app/_launch_runtime.py
@@
-# ============================================================================
-# API Routes: lightweight video editor
-# ============================================================================
-
-_video_editor_jobs: dict[str, dict] = {}
-_video_editor_jobs_lock = threading.RLock()
-_VIDEO_EDITOR_TERMINAL = frozenset({"completed", "failed", "cancelled"})
-_VIDEO_EDITOR_FFMPEG_LANE = resource_scheduler.cpu_lane("ffmpeg")
-_VIDEO_EDITOR_EXTENSIONS = {".mp4", ".webm", ".mov", ".mkv", ".avi", ".m4v"}
-_VIDEO_EDITOR_AUDIO_EXTENSIONS = {".wav", ".mp3", ".flac", ".ogg", ".m4a", ".aac"}
+_COMIC_ANIMATIC_IMAGE_EXTENSIONS remains in launch next to _resolve_comic_animatic_image.
+
+from routers.video_editor import (
+    _VIDEO_EDITOR_FFMPEG_LANE,
+    _VIDEO_EDITOR_TERMINAL,
+    _finish_video_editor_cancelled,
+    _public_video_editor_job,
+    _register_video_editor_job,
+    _remove_video_editor_output_bundle,
+    _video_editor_cancel_requested,
+    _video_editor_job_snapshot,
+    _video_editor_job_update,
+    _video_editor_task_identity,
+    cancel_video_editor_export,
+    create_video_editor_jobs_router,
+    create_video_editor_router,
+    list_video_editor_jobs,
+)
+api.include_router(create_video_editor_router(
+    workspace_dir=_workspace_dir,
+    get_active_workspace=_get_active_workspace,
+    resolve_input_path=_resolve_model3d_input_path,
+    publish_legacy_task=lambda job, adapter: _publish_generic_legacy_task(job, adapter),
+    thumbnail_cache_dir=_MEDIA_THUMBNAIL_CACHE_DIR,
+))
@@
 from routers.comics import create_comics_animatic_router
 
 api.include_router(create_comics_animatic_router(
     workspace_dir=_workspace_dir,
     get_active_workspace=_get_active_workspace,
     video_editor_task_identity=_video_editor_task_identity,
     register_video_editor_job=_register_video_editor_job,
     video_editor_job_update=_video_editor_job_update,
     public_video_editor_job=_public_video_editor_job,
     ffmpeg_lane_key=_VIDEO_EDITOR_FFMPEG_LANE.key,
     run_comic_animatic=_run_comic_animatic,
 ))
+api.include_router(create_video_editor_jobs_router())
@@
-    with _video_editor_jobs_lock:
-        video_editor_jobs = [copy.deepcopy(job) for job in _video_editor_jobs.values()]
-    for job in video_editor_jobs:
+    for job in list_video_editor_jobs():
         if str(job.get("workspace") or "default") == workspace:
             _publish_generic_legacy_task(job, "video-editor")
@@
         if adapter == "video-editor": return cancel_video_editor_export(legacy_id)

# Exact include_router replacement (copy/paste for #353):
#
# from routers.video_editor import (
#     _VIDEO_EDITOR_FFMPEG_LANE,
#     _VIDEO_EDITOR_TERMINAL,
#     _finish_video_editor_cancelled,
#     _public_video_editor_job,
#     _register_video_editor_job,
#     _remove_video_editor_output_bundle,
#     _video_editor_cancel_requested,
#     _video_editor_job_snapshot,
#     _video_editor_job_update,
#     _video_editor_task_identity,
#     cancel_video_editor_export,
#     create_video_editor_jobs_router,
#     create_video_editor_router,
#     list_video_editor_jobs,
# )
# api.include_router(create_video_editor_router(
#     workspace_dir=_workspace_dir,
#     get_active_workspace=_get_active_workspace,
#     resolve_input_path=_resolve_model3d_input_path,
#     publish_legacy_task=lambda job, adapter: _publish_generic_legacy_task(job, adapter),
#     thumbnail_cache_dir=_MEDIA_THUMBNAIL_CACHE_DIR,
# ))
# api.include_router(create_comics_animatic_router(...unchanged...))
# api.include_router(create_video_editor_jobs_router())
#
# Delete launch handlers:
#   probe_video_editor_source, probe_video_editor_audio_source,
#   serve_video_editor_thumbnail, capture_video_editor_frame,
#   start_video_editor_export, get_video_editor_export,
#   cancel_video_editor_export (local), _run_video_editor_export,
#   _resolve_video_editor_source, _resolve_video_editor_audio_source,
#   and the in-process _video_editor_jobs store.
