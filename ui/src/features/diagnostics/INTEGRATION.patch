--- a/app/_launch_runtime.py
+++ b/app/_launch_runtime.py
@@
 from routers.image_generation_commands import create_image_generation_commands_router, image_command_catalog, image_command_handlers
+from routers.user_diagnostics import create_user_diagnostics_router
@@
 api.include_router(create_image_generation_commands_router(_image_generation_commands))
+api.include_router(create_user_diagnostics_router())
 api.include_router(create_wangp_mcp_router(

--- a/ui/src/components/SettingsDrawer/SettingsDrawer.tsx
+++ b/ui/src/components/SettingsDrawer/SettingsDrawer.tsx
@@
+import { DiagnosticsPanel, diagnosticsCopy } from '../../features/diagnostics'
@@
   const tabs = [
     { id: 'performance' as const, label: t('tabs.performance') },
     { id: 'integrations' as const, label: t('tabs.integrations') },
   ]
+  const support = diagnosticsCopy()
@@
           {settingsTab === 'integrations' && (
             <ServicesSettingsPanel />
           )}
+          <div className="border-t border-border pt-4">
+            <DiagnosticsPanel />
+            <p className="sr-only">{support.tab}</p>
+          </div>
         </div>

This patch is not applied. H18 does not edit _launch_runtime.py, pinokio.js or
SettingsDrawer. Tests mount the router directly. Optional task/receipt loaders:

    api.include_router(create_user_diagnostics_router(
        load_task=lambda task_id: registry.get(task_id),
        load_receipt=lambda workspace, intent_id: registry.command_admission(intent_id),
    ))
